# Manual Describe Example

Shows `txn_describe` + `txn::field` for custom/renamed keys. The manual descriptor takes precedence over auto-reflection field names.

## Run

```sh
cd examples/manual-describe
mise exec -- exon sync
mise exec -- exon build
mise exec -- exon run
```
