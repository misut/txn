[package]
name = "txn-manual-describe"
version = "0.1.0"
description = "Manual describe example for the txn serialization library (custom keys via txn_describe)"
type = "bin"
standard = 23

[sync]
cmake-in-root = false

[dependencies.path]
txn = "../.."
